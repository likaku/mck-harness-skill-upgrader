---
name: harness-skill-upgrader
description: >-
  Harness Engineering 顾问 — 帮助用户将现有 AI Skill 从「vibe coding 模式」升级为
  「工程化 Harness 模式」。通过 5-7 轮结构化问答（结合七步成诗与战略十问核心理念）
  精准诊断当前 Skill 的上下文质量、流程完整性、门禁机制、经验沉淀四大维度缺口，
  再结合 Harness Engineering 原则输出可直接执行的改造 Action Plan。
  当用户说「帮我优化/改造这个 skill」「这个 skill 总出错」「帮我 harness 化这个 skill」
  「我的 skill 质量不稳定」「让 AI 按规矩做事」时触发。
description_zh: Skill Harness 化改造顾问
description_en: Harness Engineering upgrade advisor for AI skills
disable: false
agent_created: true
---

# Harness Skill Upgrader

> 把任意 AI Skill 从「感觉驱动」升级为「工程约束驱动」的顾问 skill。
> 通过 5-7 轮结构化问答诊断核心缺口，输出专属 Harness 改造 Action Plan。
> 底层方法论：**七步成诗**（问题界定→分解→优先化→分析→综合→输出）× **战略十问**（赢的判断、可持续性、机制 vs 信念）

---

## When to Use

- 用户想改造/优化某个现有 skill，让 AI 输出更稳定、更可预测
- 用户抱怨某个 skill「总出错」「每次结果不一样」「AI 不按规矩做事」
- 用户想把自己开发的 skill 工程化，建立流程和质量门禁
- 用户明确提到「Harness」「工程化」「让 skill 更可靠」

---

## 核心理念（每次诊断前必内化）

### 为什么需要 Harness Engineering？

**核心公式**：AI 输出质量 = AI 能力 × 上下文质量

这是乘法，不是加法。上下文质量趋近于零时，AI 能力再强输出也是零。
大多数 skill 的问题不在 AI 能力不够，而在**上下文质量太低**。

### Vibe Coding 的三个结构性缺陷

| 缺陷 | 表现 | 根因 |
|------|------|------|
| **信息损耗** | 同一 skill 每次运行结果不同 | 需求/约束未显式化成工程制品 |
| **知识孤岛** | AI 不知道历史决策、领域规范、踩坑经验 | 知识散落在聊天记录里，无法跨会话复用 |
| **验证断档** | 「能跑」就算完成，错误悄悄累积 | 门禁是 AI 口头判断，不是机读规则 |

### Harness Engineering 四大组件

```
① 上下文工程    — 知识分层、渐进披露、按需加载（解决「信息损耗」）
② 运行时控制    — 多阶段流程、状态持久化、断点恢复（解决「每次不一样」）
③ 门禁机制      — 机读化检查、结构化 checklist、程序派生 passed（解决「验证断档」）
④ 生命周期管理  — 经验沉淀、Self-Refinement 闭环、知识资产化（解决「知识孤岛」）
```

### 两条铁律

**铁律 1：LLM 把生成「完成声明」误当作完成了**
门禁必须是程序运行的结果（结构化 JSON 文件），不能是 AI 口头判断。
`passed` 是程序输出的布尔值，不是 AI 的感受。

**铁律 2：prompt 是软的，机制是硬的**
能用脚本/代码/结构化 JSON 约束的，就不要靠 prompt 要求 AI 自觉。
反模式要具象化写在 SKILL.md 顶部，不是泛化说「请遵守规则」。

### 门禁设计的核心原则（来自战略十问：资源优先配置）

不是「门禁越多越好」，而是**在代价最不对称的拐点上设硬门禁**。

```
修改代价：删几行文字 << 重新执行 << 用户发现后重做
门禁位置：内容准备阶段  <  执行后检查  <  交付后发现
结论：把硬门禁设在「修改代价最低 × 错误影响最大」的交叉点
```

---

## 诊断流程：5-7 轮问答

### 底层框架说明

本诊断将**七步成诗**与**战略十问**融合为一套对话结构：

- **七步成诗映射**：R1 界定问题 → R2-R5 分解问题 → R6 战略优先 → R7 问题陈述（综合）→ 输出改造方案
- **战略十问贯穿**：在 R6 集中校准「赢的判断」「机制可持续性」「关键约束」「速度 vs 质量权衡」

每一轮的答案决定下一轮问什么，是**对话式深挖**，不是线性填表。

---

### Round 1：界定 Skill 的产品定位
**七步成诗第一步：界定问题**

**目标**：搞清楚这个 skill 解决什么问题、服务谁、产出什么。

向用户提问（选 2-3 个最相关的）：
```
1. 这个 skill 的核心任务是什么？（一句话）
2. 它的「用户」是谁？（AI 自己 / 开发者 / 业务用户？）
3. 它的「产出物」是什么？（代码 / 文件 / 内容 / 操作 / 分析？）
4. 触发它的典型场景是什么？（用户说什么，或发生什么事情时被调用？）
5. 它现在「最常出错」或「最不稳定」的地方是什么？
```

**诊断信号**：
- 产出物定义模糊 → 上下文工程优先
- 触发场景不清晰 → description 重写优先
- 「最常出错」集中在某类操作 → 对应建门禁

---

### Round 2：解剖上下文质量
**七步成诗第二步：分解问题——上下文维度**

**目标**：找出「AI 能力 × 上下文质量」公式里，上下文质量的具体缺口。

向用户提问：
```
1. 这个 skill 的 SKILL.md 大概有多少行？
2. 每次调用都全量加载 SKILL.md，还是有按需加载机制？
3. skill 里有没有「不同场景需要不同知识」的情况？
4. 有没有以下知识目前「散落在聊天里」没有结构化：
   - 领域规范/约定（格式要求、命名规则、边界条件）
   - 历史决策（为什么选了方案 A 而不是 B）
   - 踩坑经验（某类操作曾经出过什么问题）
5. AI 是否经常在「猜」某些东西，而不是「查表」？
```

**诊断框架**：
```
上下文缺口类型：
□ 全量加载（SKILL.md 过大，每次全读）→ 需要三层知识分层
□ 知识孤岛（规范/决策/经验未结构化）→ 需要 references/ 目录
□ 能力边界不清（AI 猜参数格式、猜 API 限制）→ 需要能力矩阵（capability-matrix）
□ 经验未持久化（踩坑修完就丢）→ 需要 experiences/ 目录
```

---

### Round 3：审查流程完整性
**七步成诗第三步：分解问题——流程维度**

**目标**：找出执行流程是「一步直达」还是「有结构的多阶段」。

向用户提问：
```
1. 这个 skill 执行时大概有几个步骤？（描述一下主流程）
2. 有没有「前置准备阶段」？（输入收集、依赖检查、参数验证）
3. 有没有「中间确认点」？（执行到一半，让用户确认方向再继续）
4. 最常见的错误在哪个阶段被发现？
   - 执行前（参数格式错、缺少信息）
   - 执行中（调用失败、逻辑错误）
   - 执行后（输出有问题，需要重做）
5. 有没有「修改代价极高」的操作？（一旦开始就难以中途改方向）
```

**诊断框架**：
```
流程缺口类型：
□ 一步直达（无前置阶段）→ 增加 S1 输入收集/参数验证阶段
□ 无中间确认点 → 在「修改代价最低的拐点」前插入门禁
□ 错误在执行后才发现 → 前移门禁位置
□ 无断点恢复机制 → 需要状态文件（状态驱动而非对话驱动）
```

---

### Round 4：解剖门禁现状
**七步成诗第四步：分解问题——验证维度**

**目标**：找出当前门禁是「AI 口头」还是「机读强制」。

向用户提问：
```
1. 这个 skill 有没有「检查」动作？（参数验证、质量检查、输出确认）
2. 这些检查是：
   A. AI 在脑子里判断（「看起来对」「应该没问题」）
   B. 执行代码/脚本，读取输出结果来判断
3. 检查「失败」时，AI 会怎么做？
   A. 继续执行（忽略或自行修正）
   B. 停下来报告失败
   C. 自动修复，修完才继续
4. 有没有「AI 给自己写完成证书」的情况？
   （说「质量检查通过」，但实际没运行任何检查代码）
5. 如果要加一个「最重要的门禁」，你会加在哪里？
```

**诊断框架**：
```
门禁缺口类型：
□ 全靠 AI 口头判断 → 需要 gate_check 脚本，输出机读 JSON
□ 门禁结论无法追溯 → 需要写入文件（而不是打印到控制台）
□ 豁免权在 AI 嘴里 → 需要 WHITELIST 枚举，每条附文字证据
□ 完成声明先于完成证书 → SKILL.md 顶部加反模式警告
```

---

### Round 5：诊断经验沉淀
**七步成诗第五步：分解问题——经验维度**

**目标**：找出知识是「走一次算一次」还是「每次都在积累」。

向用户提问：
```
1. 这个 skill 出错后，修复流程是什么？
   A. 修完就继续，不记录
   B. 手动记在某个文档里
   C. 自动写入 skill 的经验文件，下次自动加载
2. 有没有「同一个问题反复出现」的情况？
3. 这个 skill 被不同会话/不同用户使用时，每次都要「从头教 AI 规则」吗？
4. 有没有「只有这个 skill 特有的知识」没有沉淀？
   （某个外部服务的特殊行为、某个数据格式的例外情况）
5. 如果这个 skill 明天被一个全新会话使用，他们需要重新踩哪些坑？
```

**诊断框架**：
```
经验沉淀缺口：
□ 修完即丢 → 需要 experiences/ 目录 + Self-Refinement 协议
□ 同一错误反复出现 → experiences/ 存在，但 SKILL.md 没有读取入口
□ 跨会话知识断裂 → 明确「哪个阶段必须读 experiences/」
□ 特有知识未结构化 → 需要 references/project/ 层
```

---

### Round 6：战略十问校准优先级
**战略十问：赢的判断 × 可持续性 × 关键约束**

**目标**：校准改造优先级，避免「为了 Harness 而 Harness」。

选取 3 个最相关的问题：

```
① 【赢的判断】改造之后，这个 skill 成功的标准是什么？
   输出更稳定 / 错误率下降 / 用户满意度提升 / 可维护性增强？

② 【真实市场】这个 skill 的使用频率是多少？
   每天高频使用 vs 偶尔用 → 决定改造投入是否值得

③ 【可持续能力】哪些改造是一次投入、持续生效的？
   哪些是每次都要重做的？优先做前者。

④ 【关键约束】有没有不能改动的东西？
   必须兼容某个平台 / 某段代码不能动 / 某个输出格式不能变

⑤ 【速度 vs 质量】用户更在意「快速出结果」还是「结果稳定可靠」？
   决定 Fast Track 条件的设计
```

---

### Round 7：问题陈述确认
**七步成诗第一步的闭环：正式确认问题陈述**

在输出改造方案之前，用以下格式做问题陈述，并请用户确认：

```
「我们要解决的核心问题是：

 [Skill名称] 在 [什么场景] 下，[具体表现出什么问题]，
 导致 [谁] 无法 [做什么]，

 根因诊断：
 - 上下文质量：[🔴 高缺口 / 🟡 中 / 🟢 低] — [一句话描述]
 - 流程完整性：[🔴 / 🟡 / 🟢] — [一句话描述]
 - 门禁机制：  [🔴 / 🟡 / 🟢] — [一句话描述]
 - 经验沉淀：  [🔴 / 🟡 / 🟢] — [一句话描述]

 改造优先顺序：[P0] → [P1] → [P2] → [P3（可选）]」

→ 用户确认后，进入输出改造方案阶段。
```

---

## 改造方案输出

完成 5-7 轮问答并获得用户确认后，使用 `templates/action-plan.md` 模板输出改造方案。

核心结构：

```
① 诊断摘要（四维缺口评级 + 核心问题陈述）
② P0 反模式警告（10分钟，立刻生效）
③ P1 门禁机读化（1-3小时，消除最大不确定性）
④ P2 知识分层（2-4小时，降低上下文噪音）
⑤ P3 Self-Refinement 闭环（可选）
⑥ 快速验证 Checklist
⑦ A/B 测试方案
```

---

## 改造执行原则

### 1. 执行层不动，只改治理层

skill 的核心逻辑（引擎代码、数据处理、API 调用）是「手脚」，不要动。
改造的是「大脑」（SKILL.md 的知识结构）和「缰绳」（门禁脚本）。

### 2. 门禁设在代价最不对称的拐点

```
修改代价：编辑文字 << 重新执行 << 用户反馈后重做
门禁应在：内容准备前  <  执行后  <  交付后
```

### 3. 反模式要具象化，不要泛化

❌ 泛化：「请遵守格式规范」
✅ 具象：「在 gate_check 脚本输出 passed:true 之前，不允许对用户说出任何完成类措辞」

### 4. 豁免权在代码里，不在 AI 嘴里

任何需要「豁免」的错误类别，必须写进代码白名单（WHITELIST 枚举），每条附文字证据。
AI 口头声明「这个错误可以忽略」永远无效。

### 5. 三层知识，渐进加载

```
team/      最稳定层 — 跨场景通用规范（加载频率：每次）
framework/ 框架层   — 跨任务通用知识（加载频率：大部分场景）
project/   项目层   — 场景特有知识（加载频率：按需）
```
每个执行阶段只加载当前阶段需要的层，不全量加载。

---

## Pitfalls（常见陷阱）

- **过度设计**：使用频率低的 skill，P0 反模式警告就够了，不需要全套 4 个组件
- **门禁太多**：门禁是摩擦，加多了 AI 会绕过；只在「代价最不对称」的节点设硬门禁
- **SKILL.md 越改越长**：Harness 化后 SKILL.md 应该变短（入口+索引），详细知识分散到 references/
- **experiences/ 没有读取入口**：目录建了但 SKILL.md 没有写「第 X 阶段必须读 experiences/*.md」——AI 不会主动读
- **忘记写反模式**：最容易被忽略，但效果最立竿见影；具象化的反模式比正面规则管用 10 倍
- **跳过问题陈述确认**：Round 7 是整个诊断的锚点，跳过会导致改造方向与用户真实需求偏离

---

## Verification（验证改造效果）

改造完成后，用相同提示词跑两次（改造前 vs 改造后），对比：

| 指标 | 测量方式 | 期望方向 |
|------|---------|---------|
| 首轮运行错误数 | 记录运行时报错次数 | ↓ 减少 |
| gate passed 需要几轮 | 几轮才能得到 passed:true | ↓ 减少 |
| 输出洞见密度 | 完整句子 vs 标签词的比例 | ↑ 增加 |
| 同类错误复现率 | 第二次运行是否踩同一个坑 | = 0 |
| 上下文消耗 | token 数（可测量时）| ↓ 减少 |
